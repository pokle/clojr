#!/usr/bin/env bash

# Publish to Clojars. Requires CLOJARS_USERNAME and CLOJARS_PASSWORD
# (a deploy token) in the environment, or credentials in
# ~/.lein/credentials.clj.gpg under {#"clojars" {:username ... :password ...}}.

set -euo pipefail

cd "$(dirname "$0")/.."

lein deploy clojars
